// ========== ИГРОВЫЕ КОНСТАНТЫ LEGEND OF WORLDS ==========
// Единый источник правды для всех калькуляторов и сравнения.
// Не менять в отдельных файлах — только здесь.

const GAME_CONSTANTS = {

  // ── Броня ──
  ARMOR_C: 0.05,              // Константа снижения урона (не 0.06 как в стандартном WC3)
  AGI_ARMOR_FACTOR: 0.05,     // Ловкость → броня: +0.05 за единицу (= 1 броня за 20 ловкости)

  // ── Атрибуты ──
  STR_HP_FACTOR: 20,          // Сила → здоровье: +20 HP за единицу
  INT_MP_FACTOR: 15,          // Разум → мана: +15 маны за единицу
  AGI_ATK_SPEED_FACTOR: 0.01,// Ловкость → скорость боя: +0.01 за единицу

  // ── Регенерация ──
  STR_HP_REGEN_FACTOR: 0.05,  // Сила → реген HP: +0.05/сек за единицу
  INT_MP_REGEN_FACTOR: 0.05,  // Разум → реген MP: +0.05/сек за единицу
  MP_REGEN_BASE: 0.01,        // Базовый реген маны (до бонусов от разума)

  // ── Порча ──
  CORRUPTION_BONUS: 0.20,     // Бонус урона за стак порчи: +20%

  // ── Снижение лечения ──
  // Не складывается — берётся наибольшее значение

  // ── Блок урона ──
  // Не складывается — берётся наивысший шанс
};

// Формула брони: Снижение = (Броня × C) / (1 + Броня × C)
// Формула удачи: Итог = Базовый% + Удача × (1 − Базовый% / 100)
// Формула мультипликативная: Итого = 1 − (1−a) × (1−b) × (1−c)...
// Формула аддитивная: Итого = a + b + c...
